
(security_headers) {
    header {
        defer
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
        -Server
    }
}

# ┌──────────────────────────────────────────────────┐
# │                        VPS                       │
# └──────────────────────────────────────────────────┘

# ────────── CADDY ──────────

www.mydomain.net {
	import security_headers
    redir https://mydomain.net{uri}
}

mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }

    @api path /api*
    reverse_proxy @api wireguard:4000

    reverse_proxy front:80
}

# ────────── AUTHELIA ──────────

auth.mydomain.net {
	import security_headers
    reverse_proxy authelia:9091
}

# ────────── PORTAINER ──────────

portainer.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy portainer:9000
}

# ────────── UPTIME KUMA ──────────

kuma.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy uptime-kuma:3001
}

# ────────── GUACAMOLE ──────────

guacamole.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }

    @root path /
    redir @root /guacamole/ 302

    reverse_proxy guacamole:8080 {
        header_up Remote-Email {http.request.header.Remote-Email}
        header_up Remote-Name {http.request.header.Remote-Name}
        header_up Remote-User {http.request.header.Remote-User}
        header_up Remote-Groups {http.request.header.Remote-Groups}
    }
}

# ────────── FILE BROWSER ──────────

files.vps.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy filebrowser-vps:8085
}

# ────────── NAVIDROME ──────────

navidrome.mydomain.net {
	import security_headers
    handle /rest/* {
        reverse_proxy navidrome:4533
    }

    handle {
        forward_auth authelia:9091 {
            uri /api/authz/forward-auth
            copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
        }
        reverse_proxy navidrome:4533
    }
}

# ────────── KAVITA ──────────

kavita.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy kavita:5000
}

# ────────── ROMM ──────────

romm.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy romm:8080
}

# ┌──────────────────────────────────────────────────┐
# │                        RPI                       │
# └──────────────────────────────────────────────────┘

# ────────── FILE BROWSER ──────────

files.rpi.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:8086
}

# ────────── TORRENT ──────────

torrent.rpi.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:8899
}

# ┌──────────────────────────────────────────────────┐
# │                        PC                        │
# └──────────────────────────────────────────────────┘

# ────────── FILE BROWSER ──────────

files.pc.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:8085
}

# ────────── VSCODE ──────────

vscode.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
	reverse_proxy wireguard:7777
}

# ────────── NAVIDROME ──────────

navidrome.pc.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
	reverse_proxy wireguard:7775
}

# ────────── JELLYFIN ──────────

jellyfin.mydomain.net {
	import security_headers
    reverse_proxy wireguard:8096
}

# ────────── TORRENT ──────────

torrent.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:8898
}

# ────────── SUNSHINE ──────────

sunshine.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy https://wireguard:47990 {
		transport http {
			tls_insecure_skip_verify
		}
	}
}

# ────────── MOONLIGHT ──────────

moonlight.mydomain.net {
	import security_headers
	forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:7891
}

# ────────── ACE STEP ──────────

acestep.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy wireguard:7860
}

# ────────── COMFYUI ──────────

comfyui.mydomain.net {
	import security_headers
    encode zstd gzip

    @websockets {
        header Upgrade websocket
    }

    @assets {
        path /assets/*
    }

    handle @websockets {
        reverse_proxy wireguard:8188 {
            flush_interval -1
        }
    }

    handle @assets {
        reverse_proxy wireguard:8188 {
            flush_interval -1
        }
    }

    handle {
        forward_auth authelia:9091 {
            uri /api/authz/forward-auth
            copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
        }
        reverse_proxy wireguard:8188 {
            flush_interval -1
        }
    }
}

# ────────── OPEN WEBUI ──────────

openwebui.mydomain.net {
	import security_headers

    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }

    reverse_proxy wireguard:8081 {
        header_up Remote-Email {http.request.header.Remote-Email}
        header_up Remote-Name {http.request.header.Remote-Name}
        header_up Remote-User {http.request.header.Remote-User}
        header_up Remote-Groups {http.request.header.Remote-Groups}
    }
}

# ────────── SEARXNG ──────────

searxng.mydomain.net {
	import security_headers
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy searxng:8888
}
